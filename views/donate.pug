extends layout

block content
    div(style="position: relative; top: 30vh; left: 20vw; width: 50vw")
        div.box
            div.field
                label.label Amount:
                input#amount.input.is-primary.is-focused(type='number', min='0', placeholder="Donation Amount")
            div.control
                button#submit.button.is-link Donate